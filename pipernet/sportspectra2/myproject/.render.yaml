services:
  - type: web
    name: my-django-app
    runtime: python
    buildCommand: "./.render/render-build.sh && pip install -r requirements.txt && python manage.py collectstatic --no-input && python manage.py migrate"
    startCommand: "gunicorn myproject.wsgi"
